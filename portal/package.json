{
  "name": "portal",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx http-serve",
    "build": "npm run compress",
    "compress": "node ./util/compress.js"
  },
  "devDependencies": {
    "@gfx/zopfli": "^1.0.15"
  }
}
